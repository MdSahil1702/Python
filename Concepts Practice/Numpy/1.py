import numpy as np #importing numpy package

a=np.array([1,2,3,4,5]) # giving the list as an parameter to the numpy method array 

print(a) # the provided list is converted to array 

print(type(a)) # this will confirm that list is converted to ndarray of numpy